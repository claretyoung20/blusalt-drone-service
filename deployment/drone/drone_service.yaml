apiVersion: v1
kind: Service
metadata:
  name: droneservice
  namespace: blusalt
  labels:
    app: drone-svc
spec:
  selector:
    app: drone-service
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8060
